{
    WebResponse response;
    response.setHeader(string, string);
    response.setHeader(string, string);
    response.setDateHeader(string, number);
    response.setContentType(string);
    response.setContentLength(getImageData().length);
    response.setAjax(boolean);
}

 file name 377.java

 duplicated lines 4

 total time 0